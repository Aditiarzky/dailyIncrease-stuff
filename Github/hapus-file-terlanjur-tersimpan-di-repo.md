pertama buat file .gitignore contoh isinya seperti:<br>
<code>
node_modules/
.vscode/
</code>
<br>
setelah itu jalankan perintah:
git rm -r --cached namafolder
-untuk hapus foldernya.
setelah itu di commit dan push
